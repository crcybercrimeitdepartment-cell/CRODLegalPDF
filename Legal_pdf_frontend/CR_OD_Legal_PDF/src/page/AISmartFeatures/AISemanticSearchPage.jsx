import React from 'react';
import ToolWorkspace from '../../ToolWorkspace';

export default function AISemanticSearchPage({ tool, onBack }) {
  return (
    <ToolWorkspace tool={tool} onBack={onBack} />
  );
}
