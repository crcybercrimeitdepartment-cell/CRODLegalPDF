import React from 'react';
import ToolWorkspace from '../../ToolWorkspace';

export default function ExtractKeyPointsPage({ tool, onBack }) {
  return (
    <ToolWorkspace tool={tool} onBack={onBack} />
  );
}
