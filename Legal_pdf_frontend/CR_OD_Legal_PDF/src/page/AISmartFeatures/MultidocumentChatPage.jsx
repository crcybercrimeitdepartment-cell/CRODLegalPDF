import React from 'react';
import ToolWorkspace from '../../ToolWorkspace';

export default function MultidocumentChatPage({ tool, onBack }) {
  return (
    <ToolWorkspace tool={tool} onBack={onBack} />
  );
}
