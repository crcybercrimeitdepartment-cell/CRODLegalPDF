import React from 'react';
import ToolWorkspace from '../../ToolWorkspace';

export default function TranslatePDFPage({ tool, onBack }) {
  return (
    <ToolWorkspace tool={tool} onBack={onBack} />
  );
}
