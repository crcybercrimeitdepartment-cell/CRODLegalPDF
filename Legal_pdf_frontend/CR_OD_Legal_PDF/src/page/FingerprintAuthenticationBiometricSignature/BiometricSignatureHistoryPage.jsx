import React from 'react';
import ToolWorkspace from '../../ToolWorkspace';

export default function BiometricSignatureHistoryPage({ tool, onBack }) {
  return (
    <ToolWorkspace tool={tool} onBack={onBack} />
  );
}
