import React from 'react';
import ToolWorkspace from '../../ToolWorkspace';

export default function ReplaceFingerprintPage({ tool, onBack }) {
  return (
    <ToolWorkspace tool={tool} onBack={onBack} />
  );
}
