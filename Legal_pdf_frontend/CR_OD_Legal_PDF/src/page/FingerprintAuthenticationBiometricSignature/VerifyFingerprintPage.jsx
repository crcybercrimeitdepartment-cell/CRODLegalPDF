import React from 'react';
import ToolWorkspace from '../../ToolWorkspace';

export default function VerifyFingerprintPage({ tool, onBack }) {
  return (
    <ToolWorkspace tool={tool} onBack={onBack} />
  );
}
