import React from 'react';
import ToolWorkspace from '../../ToolWorkspace';

export default function AISecurityRecommendationsPage({ tool, onBack }) {
  return (
    <ToolWorkspace tool={tool} onBack={onBack} />
  );
}
