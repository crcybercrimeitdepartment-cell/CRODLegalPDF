import React from 'react';
import ToolWorkspace from '../../ToolWorkspace';

export default function SignatureEvidenceReportPage({ tool, onBack }) {
  return (
    <ToolWorkspace tool={tool} onBack={onBack} />
  );
}
