import React from 'react';
import ToolWorkspace from '../../ToolWorkspace';

export default function SignatureFieldCreatorPage({ tool, onBack }) {
  return (
    <ToolWorkspace tool={tool} onBack={onBack} />
  );
}
