import React from 'react';
import ToolWorkspace from '../../ToolWorkspace';

export default function SignaturePermissionManagerPage({ tool, onBack }) {
  return (
    <ToolWorkspace tool={tool} onBack={onBack} />
  );
}
