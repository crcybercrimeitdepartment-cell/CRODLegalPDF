import React from 'react';
import ToolWorkspace from '../../ToolWorkspace';

export default function CompareVersionsPage({ tool, onBack }) {
  return (
    <ToolWorkspace tool={tool} onBack={onBack} />
  );
}
