import React from 'react';
import ToolWorkspace from '../../ToolWorkspace';

export default function SubscriptionManagementPage({ tool, onBack }) {
  return (
    <ToolWorkspace tool={tool} onBack={onBack} />
  );
}
